[az-syd-vm]
hostname = az-syd-vm
username = kpadmin

[az-mel-vm]
hostname = az-mel-vm
username = kpadmin

[aws-syd-vm]
hostname = aws-syd-vm
username = ec2user

[aws-mel-vm]
hostname = aws-mel-vm
username = ec2user

[oci-syd-vm]
hostname = oci-syd-vm
username = opc

[oci-mel-vm]
hostname = oci-mel-vm
username = opc

[raspberrypi]
hostname = raspberrypi
username = ash

[ashs-mbp]
hostname = ashs-mbp
username = ash

[localhost]
hostname = localhost
username = ash
